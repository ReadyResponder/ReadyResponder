require 'spec_helper'

describe Channel do
  before (:each) do
    @channel = FactoryGirl.create(:channel)
  end
  describe "relationships" do
#      @channel.should respond_to?(:status)
    #c = Channel.new
    #c.respond_to?(:status).should == true
    #@channel.respond_to?(:status).should == true
      #@channel.respond_to?(:person).should == true
  end

end
