<tr>
  <td class="<%= h p.title.try(:downcase).try(:split, " ").try(:first) %> id"><%= p.icsid %></td>
  <td class="<%= h p.title.try(:downcase).try(:split, " ").try(:first) %> name">
  <%= p.shortrank + " " + p.firstname[0] + " " + p.lastname %></td>
  <td class= "signature">&nbsp</td>
  <td class= "time">&nbsp</td>
  <td class= "time">&nbsp</td>
</tr>
<p>
  Created <%= @item_category.created_at %><br/>
Last Modified <%= @item_category.updated_at %><br/> 
Last Editor <%= @last_editor %> 
</p>