<% content_for :sidebar do %>
  <h1>Editing Notification</h1>
  <%= sidebar_button_link 'Cancel', :back %>
<% end %>

<% if @event.present? %>
  <%= render 'event_form' %>
<% else %>
  <%= render 'form' %>
<% end %>
