<%= form_for @inspection_tag do |f| %>
  <%= error_messages_for(f.object) %>
  <div class="form-group">
    <%= f.label :name, "Tag Name" %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit %>
<% end %>
