<table id="assignments" class="datatable table table-striped table-bordered">
  <thead>
    <tr>
      <th>Person</th>
      <th>Assignment Status</th>
      <th>Start Time</th>
      <th>End Time</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% assignments.each do |assignment| %>
      <tr>
        <td><%= link_to assignment.person.fullname, assignment %></td>
        <td><%= assignment.status %></td>
        <td><%= assignment.start_time.strftime('%a %b %d %k:%M') %></td>
        <td><%= assignment.end_time.strftime('%a %b %d %k:%M') %></td>

        <td><%= table_button_link 'Edit',
                    edit_requirement_assignment_path(@requirement, assignment) if can? :edit, assignment %></td>
      </tr>
    <% end %>
  </tbody>
</table>
