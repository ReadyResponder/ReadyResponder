<% content_for :sidebar do %>
  <%= sidebar_button_link 'Return to Listing', users_path %>
  <%= sidebar_button_link 'Edit User', edit_user_path(@user) unless cannot? :edit, @user  %>
<% end %>

<h2><%= @user.firstname %> <%= @user.lastname %></h2>
